import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'laser-cutting-tools-online-store',
  webDir: 'www'
};

export default config;
